# Public API URL for Expo app (baked into the JS bundle)
EXPO_PUBLIC_API_URL=http://10.0.2.2:4000

# Optional: override per build profile (staging/prod) in eas.json
# EXPO_PUBLIC_API_URL=https://staging.api.greenbro.co.za
